// Solver, uses the allowed words and a secret word to try to guess.

pub struct Solver {
    allowed: Vec<String>,
}

impl Solver {
    pub fn solve(_secret: String) -> Vec<String> {
        let o: Vec<String> = Vec::new();
        o
    }
}